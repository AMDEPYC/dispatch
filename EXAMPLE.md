The `example` release contains binaries that do nothing but reboot. This is
useful for testing `dispatch` against a known working workflow.

## 🚀 Quick Start

```bash
dispatch --owner AMDEPYC --repo dispatch --tag example
```
